{
  "name": "iam-crm-frontend-shadcn",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "npm run test --workspace web",
    "test:run": "npm run test:run --workspace web",
    "test:coverage": "npm run test:coverage --workspace web",
    "build": "turbo build",
    "analyze": "npm run analyze --workspace web",
    "bundle:check": "npm run bundle:check --workspace web",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.9.18",
    "typescript": "~6"
  },
  "packageManager": "npm@11.0.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "date-fns": "^4.4.0",
    "jalaali-js": "^2.0.1"
  }
}
