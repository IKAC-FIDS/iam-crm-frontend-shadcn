/**
 * When enabled, supported list pages are locked to the desktop viewport and
 * their result panel owns the vertical scrollbar. Keep disabled for natural
 * document scrolling, which is safer on short desktop and laptop displays.
 */
export const CONTAINED_PAGE_SCROLL_ENABLED = false

